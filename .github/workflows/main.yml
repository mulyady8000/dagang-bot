name: Collect market data

on:
  schedule:
    - cron: '*/5 * * * *'   # every 5 minutes (GitHub Actions minimum)
  workflow_dispatch: {}      # manual trigger button in the Actions tab

permissions:
  contents: write             # needed to commit data back

concurrency:
  group: collect
  cancel-in-progress: false

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip

      - run: pip install -r requirements.txt

      - run: python cloud_collect.py

      - name: Commit new data
        run: |
          git config user.name "data-bot"
          git config user.email "data-bot@users.noreply.github.com"
          git add data/
          if git diff --cached --quiet; then
            echo "no new data to commit"
          else
            git commit -m "data: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
            # retry push in case another scheduled run pushed first
            for i in 1 2 3; do
              if git pull --rebase --autostash origin "${GITHUB_REF_NAME}" && git push; then
                break
              fi
              sleep $((RANDOM % 5 + 1))
            done
          fi
